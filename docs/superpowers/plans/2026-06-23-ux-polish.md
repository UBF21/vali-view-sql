# UX Polish Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar 7 mejoras UI/UX independientes identificadas en el audit post-Tier 3 de vali-viewsql.

**Architecture:** Cada task toca archivos distintos — cero conflictos entre tareas. React 19 + TypeScript strict + Zustand. Test runner: `npm run test` (vitest + @testing-library/react).

**Tech Stack:** React 19, TypeScript strict, Zustand, Framer Motion, Lucide React, Vitest, @testing-library/react

---

## Task 1: Canvas Loading State

**Files:**
- Modify: `src/index.css` — agregar `@keyframes spin` y clase `.spin`
- Modify: `src/components/layout/AppShell.tsx:2,247-258` — agregar Loader2, condicionar spinner vs empty state

### Cambio requerido

**En `AppShell.tsx`** — la sección CENTER CANVAS actualmente:
```tsx
{nodes.length === 0 && !isLoading && (
  <div style={{ position: 'absolute', inset: 0, display: 'flex', flexDirection: 'column',
    alignItems: 'center', justifyContent: 'center', gap: 8, opacity: 0.4,
    pointerEvents: 'none', zIndex: 1,
  }}>
    <LayoutGrid size={36} style={{ opacity: 0.4, color: 'var(--text-2)' }} />
    <span style={{ fontSize: 13, color: 'var(--text-2)' }}>Type a SQL query to visualize it</span>
  </div>
)}
```

Debe quedar:
```tsx
{isLoading && nodes.length === 0 && (
  <div style={{ position: 'absolute', inset: 0, display: 'flex',
    alignItems: 'center', justifyContent: 'center', pointerEvents: 'none', zIndex: 1,
  }}>
    <Loader2 size={28} className="spin" style={{ color: 'var(--text-2)', opacity: 0.5 }} />
  </div>
)}
{!isLoading && nodes.length === 0 && (
  <div style={{ position: 'absolute', inset: 0, display: 'flex', flexDirection: 'column',
    alignItems: 'center', justifyContent: 'center', gap: 8, opacity: 0.4,
    pointerEvents: 'none', zIndex: 1,
  }}>
    <LayoutGrid size={36} style={{ opacity: 0.4, color: 'var(--text-2)' }} />
    <span style={{ fontSize: 13, color: 'var(--text-2)' }}>Type a SQL query to visualize it</span>
  </div>
)}
```

**En el import de AppShell.tsx** — añadir `Loader2`:
```tsx
import { Code2, AlertTriangle, BookOpen, PanelLeftClose, PanelLeftOpen,
         PanelRightClose, PanelRightOpen, X, LayoutGrid, Loader2 } from 'lucide-react'
```

**En `src/index.css`** — añadir al final del archivo:
```css
/* ── Spinner ──────────────────────────────────────────────── */
@keyframes spin {
  to { transform: rotate(360deg); }
}
.spin {
  animation: spin 1s linear infinite;
}
```

- [ ] **Step 1: Escribir el test**

Crear `src/__tests__/layout/AppShell.loading.test.tsx`:
```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen } from '@testing-library/react'
import { ReactFlowProvider } from '@xyflow/react'

// Mock ReactFlow para no romper JSDOM
vi.mock('@xyflow/react', async () => {
  const actual = await vi.importActual('@xyflow/react')
  return { ...actual, useReactFlow: () => ({ fitView: vi.fn() }) }
})

vi.mock('@/store/useAppStore', () => ({
  useAppStore: vi.fn(),
}))

import { useAppStore } from '@/store/useAppStore'
import { AppShell } from '@/components/layout/AppShell'

const BASE_STORE = {
  query: '', setQuery: vi.fn(), dialect: 'postgresql', parseResult: null,
  isLoading: false, mode: 'explain', issues: [], infoNode: null,
  suggestions: [], schema: null,
}

function renderShell(overrides = {}) {
  const store = { ...BASE_STORE, ...overrides }
  vi.mocked(useAppStore).mockImplementation((sel: (s: typeof store) => unknown) => sel(store))
  return render(<ReactFlowProvider><AppShell /></ReactFlowProvider>)
}

describe('AppShell canvas center', () => {
  beforeEach(() => { vi.clearAllMocks() })

  it('shows spinner when isLoading and no nodes', () => {
    renderShell({ isLoading: true, parseResult: null })
    // Loader2 renders as SVG — buscar por clase
    expect(document.querySelector('.spin')).toBeTruthy()
    expect(screen.queryByText('Type a SQL query to visualize it')).toBeNull()
  })

  it('shows empty state when not loading and no nodes', () => {
    renderShell({ isLoading: false, parseResult: null })
    expect(screen.getByText('Type a SQL query to visualize it')).toBeTruthy()
    expect(document.querySelector('.spin')).toBeNull()
  })

  it('shows neither spinner nor empty state when nodes exist', () => {
    renderShell({
      isLoading: false,
      parseResult: { nodes: [{ id: 'n1', type: 'table', position: { x: 0, y: 0 }, data: {} }], edges: [], glossary: [], steps: [] },
    })
    expect(document.querySelector('.spin')).toBeNull()
    expect(screen.queryByText('Type a SQL query to visualize it')).toBeNull()
  })
})
```

- [ ] **Step 2: Verificar que falla**

```
npm run test -- src/__tests__/layout/AppShell.loading.test.tsx
```

Expected: FAIL (Loader2 no está, clase .spin no existe)

- [ ] **Step 3: Agregar `.spin` a index.css**

Append al final de `src/index.css`:
```css
/* ── Spinner ──────────────────────────────────────────────── */
@keyframes spin {
  to { transform: rotate(360deg); }
}
.spin {
  animation: spin 1s linear infinite;
}
```

- [ ] **Step 4: Actualizar AppShell.tsx**

Cambiar import de lucide-react en línea 2 para agregar `Loader2`:
```tsx
import { Code2, AlertTriangle, BookOpen, PanelLeftClose, PanelLeftOpen, PanelRightClose, PanelRightOpen, X, LayoutGrid, Loader2 } from 'lucide-react'
```

Reemplazar el bloque canvas empty state (líneas 247-258) con dos bloques separados:
```tsx
{isLoading && nodes.length === 0 && (
  <div style={{ position: 'absolute', inset: 0, display: 'flex',
    alignItems: 'center', justifyContent: 'center', pointerEvents: 'none', zIndex: 1,
  }}>
    <Loader2 size={28} className="spin" style={{ color: 'var(--text-2)', opacity: 0.5 }} />
  </div>
)}
{!isLoading && nodes.length === 0 && (
  <div style={{ position: 'absolute', inset: 0, display: 'flex', flexDirection: 'column',
    alignItems: 'center', justifyContent: 'center', gap: 8, opacity: 0.4,
    pointerEvents: 'none', zIndex: 1,
  }}>
    <LayoutGrid size={36} style={{ opacity: 0.4, color: 'var(--text-2)' }} />
    <span style={{ fontSize: 13, color: 'var(--text-2)' }}>Type a SQL query to visualize it</span>
  </div>
)}
```

- [ ] **Step 5: Correr tests**

```
npm run test -- src/__tests__/layout/AppShell.loading.test.tsx
```

Expected: 3 PASS

- [ ] **Step 6: TypeScript clean**

```
npx tsc --noEmit
```

Expected: sin errores

- [ ] **Step 7: Commit**

```
git add src/index.css src/components/layout/AppShell.tsx src/__tests__/layout/AppShell.loading.test.tsx
git commit -m "feat(ux): show loading spinner on canvas while parsing query"
```

---

## Task 2: Export UX Guards + Feedback

**Files:**
- Modify: `src/hooks/useExport.ts` — remover try/catch de exportPNG y exportSVG
- Modify: `src/components/diagram/ExportButton.tsx` — guard nodes, fix título, fix texto loading, mostrar error

### Cambio requerido en `useExport.ts`

`exportPNG` actualmente tiene try/catch que traga el error. Debe lanzarlo:
```ts
const exportPNG = useCallback(async () => {
  const el = getFlowEl(canvasRef)
  if (!el) return
  const dataUrl = await toPng(el, { cacheBust: true, backgroundColor: 'var(--bg-primary)' })
  const link = document.createElement('a')
  link.download = `vali-view-sql-${Date.now()}.png`
  link.href = dataUrl
  link.click()
}, [])
```

`exportSVG` igual:
```ts
const exportSVG = useCallback(async () => {
  const el = getFlowEl(canvasRef)
  if (!el) return
  const dataUrl = await toSvg(el, { cacheBust: true })
  const link = document.createElement('a')
  link.download = `vali-view-sql-${Date.now()}.svg`
  link.href = dataUrl
  link.click()
}, [])
```

`exportReport` mantiene su try/catch interno para la captura del diagrama (no-fatal by design). Sin cambios.

### Cambio requerido en `ExportButton.tsx`

Archivo completo resultante:
```tsx
import { useState, useEffect } from 'react'
import { Download, FileText } from 'lucide-react'
import { useExport } from '@/hooks/useExport'
import { useAppStore } from '@/store/useAppStore'

type ExportType = 'png' | 'svg' | 'report'

const BTN_CONFIG: { type: ExportType; label: string; ariaLabel: string; title: string }[] = [
  { type: 'png',    label: 'PNG',    ariaLabel: 'Export diagram as PNG',      title: 'Export diagram as PNG' },
  { type: 'svg',    label: 'SVG',    ariaLabel: 'Export diagram as SVG',      title: 'Export diagram as SVG' },
  { type: 'report', label: 'Report', ariaLabel: 'Export analysis report',     title: 'Open analysis report in new tab' },
]

function btnStyle(type: ExportType, exporting: ExportType | null, disabled: boolean): React.CSSProperties {
  return {
    background: 'var(--bg-elevated)', border: '1px solid var(--border)', borderRadius: 6,
    padding: '4px 10px', cursor: (disabled || exporting) ? (disabled ? 'not-allowed' : 'wait') : 'pointer',
    fontSize: 11, fontWeight: 600,
    color: 'var(--text-secondary)', opacity: (disabled || (exporting && exporting !== type)) ? 0.45 : 1,
    transition: 'opacity 0.15s', display: 'flex', alignItems: 'center', gap: 5,
  }
}

function BtnIcon({ type, active }: { type: ExportType; active: boolean }) {
  if (active) return <span>Exporting...</span>
  const Icon = type === 'report' ? FileText : Download
  const labels: Record<ExportType, string> = { png: 'PNG', svg: 'SVG', report: 'Report' }
  return <><Icon size={13} /> {labels[type]}</>
}

export function ExportButton() {
  const { exportPNG, exportSVG, exportReport } = useExport()
  const nodes = useAppStore((s) => s.parseResult?.nodes ?? [])
  const [exporting, setExporting] = useState<ExportType | null>(null)
  const [error, setError] = useState<string | null>(null)

  useEffect(() => {
    if (!error) return
    const t = setTimeout(() => setError(null), 3000)
    return () => clearTimeout(t)
  }, [error])

  const handle = async (type: ExportType) => {
    setExporting(type)
    setError(null)
    try {
      if (type === 'png') await exportPNG()
      else if (type === 'svg') await exportSVG()
      else await exportReport()
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Export failed')
    } finally {
      setExporting(null)
    }
  }

  return (
    <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'flex-end', gap: 4 }}>
      <div style={{ display: 'flex', gap: 6 }}>
        {BTN_CONFIG.map(({ type, ariaLabel, title }) => {
          const isPngOrSvg = type === 'png' || type === 'svg'
          const disabled = isPngOrSvg && nodes.length === 0
          const disabledTitle = disabled ? 'Diagram is empty — type a SQL query first' : title
          return (
            <button
              key={type}
              onClick={() => !disabled && handle(type)}
              disabled={!!exporting || disabled}
              aria-label={ariaLabel}
              title={disabledTitle}
              style={btnStyle(type, exporting, disabled)}
            >
              <BtnIcon type={type} active={exporting === type} />
            </button>
          )
        })}
      </div>
      {error && (
        <span role="alert" style={{ fontSize: 10, color: '#E24B4A', maxWidth: 200, textAlign: 'right' }}>
          {error}
        </span>
      )}
    </div>
  )
}
```

- [ ] **Step 1: Escribir tests**

Crear `src/__tests__/diagram/ExportButton.test.tsx`:
```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'

vi.mock('@/hooks/useExport', () => ({
  useExport: () => ({
    exportPNG: vi.fn().mockRejectedValue(new Error('canvas error')),
    exportSVG: vi.fn(),
    exportReport: vi.fn(),
  }),
}))

vi.mock('@/store/useAppStore', () => ({
  useAppStore: vi.fn(),
}))

import { useAppStore } from '@/store/useAppStore'
import { ExportButton } from '@/components/diagram/ExportButton'

function renderWithNodes(nodeCount: number) {
  vi.mocked(useAppStore).mockImplementation(
    (sel: (s: { parseResult: { nodes: unknown[] } | null }) => unknown) =>
      sel({ parseResult: nodeCount > 0 ? { nodes: new Array(nodeCount).fill({}) } : null })
  )
  return render(<ExportButton />)
}

describe('ExportButton', () => {
  beforeEach(() => vi.clearAllMocks())

  it('disables PNG and SVG when diagram is empty', () => {
    renderWithNodes(0)
    const [png, svg] = screen.getAllByRole('button')
    expect(png).toBeDisabled()
    expect(svg).toBeDisabled()
    // report button should not be disabled
    expect(screen.getAllByRole('button')[2]).not.toBeDisabled()
  })

  it('enables all buttons when diagram has nodes', () => {
    renderWithNodes(2)
    screen.getAllByRole('button').forEach(btn => expect(btn).not.toBeDisabled())
  })

  it('shows "Diagram is empty" title on disabled PNG/SVG', () => {
    renderWithNodes(0)
    const [png] = screen.getAllByRole('button')
    expect(png.title).toBe('Diagram is empty — type a SQL query first')
  })
})
```

- [ ] **Step 2: Verificar que falla**

```
npm run test -- src/__tests__/diagram/ExportButton.test.tsx
```

Expected: FAIL

- [ ] **Step 3: Actualizar useExport.ts**

Remover try/catch de `exportPNG` y `exportSVG`. El archivo queda:
```ts
import { useCallback, useRef } from 'react'
import { toPng, toSvg } from 'html-to-image'
import { useAppStore } from '@/store/useAppStore'
import { generateReportHTML } from '@/lib/report/report-generator'

function getFlowEl(ref: React.RefObject<HTMLDivElement | null>): HTMLElement | null {
  return ref.current ?? (document.querySelector('.react-flow') as HTMLElement | null)
}

function useExportDiagram() {
  const canvasRef = useRef<HTMLDivElement | null>(null)

  const exportPNG = useCallback(async () => {
    const el = getFlowEl(canvasRef)
    if (!el) return
    const dataUrl = await toPng(el, { cacheBust: true, backgroundColor: 'var(--bg-primary)' })
    const link = document.createElement('a')
    link.download = `vali-view-sql-${Date.now()}.png`
    link.href = dataUrl
    link.click()
  }, [])

  const exportSVG = useCallback(async () => {
    const el = getFlowEl(canvasRef)
    if (!el) return
    const dataUrl = await toSvg(el, { cacheBust: true })
    const link = document.createElement('a')
    link.download = `vali-view-sql-${Date.now()}.svg`
    link.href = dataUrl
    link.click()
  }, [])

  return { exportPNG, exportSVG, canvasRef }
}

function useExportReport(canvasRef: React.RefObject<HTMLDivElement | null>) {
  const query            = useAppStore((s) => s.query)
  const dialect          = useAppStore((s) => s.dialect)
  const issues           = useAppStore((s) => s.issues)
  const suggestions      = useAppStore((s) => s.suggestions)
  const columnLineage    = useAppStore((s) => s.columnLineage)
  const complexityResult = useAppStore((s) => s.complexityResult)

  const exportReport = useCallback(async () => {
    let diagramDataUrl: string | null = null
    try {
      const el = getFlowEl(canvasRef)
      if (el) diagramDataUrl = await toPng(el, { backgroundColor: '#0d0d12', pixelRatio: 1.5 })
    } catch { /* capture failure is non-fatal */ }

    const html = generateReportHTML({
      sql: query, dialect, diagramDataUrl,
      complexity: complexityResult, issues, suggestions, lineage: columnLineage,
    })

    const win = window.open('', '_blank')
    if (win) { win.document.write(html); win.document.close() }
  }, [query, dialect, issues, suggestions, columnLineage, complexityResult, canvasRef])

  return { exportReport }
}

export function useExport() {
  const { exportPNG, exportSVG, canvasRef } = useExportDiagram()
  const { exportReport } = useExportReport(canvasRef)
  return { exportPNG, exportSVG, canvasRef, exportReport }
}
```

- [ ] **Step 4: Reemplazar ExportButton.tsx**

Reemplazar el contenido completo de `src/components/diagram/ExportButton.tsx` con el código mostrado en "Cambio requerido en ExportButton.tsx" más arriba.

- [ ] **Step 5: Correr tests**

```
npm run test -- src/__tests__/diagram/ExportButton.test.tsx
```

Expected: 3 PASS

- [ ] **Step 6: Suite completa**

```
npm run test
```

Expected: todos pasan

- [ ] **Step 7: TypeScript**

```
npx tsc --noEmit
```

- [ ] **Step 8: Commit**

```
git add src/hooks/useExport.ts src/components/diagram/ExportButton.tsx src/__tests__/diagram/ExportButton.test.tsx
git commit -m "feat(ux): export guards — disable on empty canvas, surface errors, fix loading text"
```

---

## Task 3: ConversionModal — Auto-focus + Aria-live en Copy

**Files:**
- Modify: `src/components/editor/ConversionModal.tsx` — auto-focus al abrir, aria-live en Copy button

### Cambios requeridos

**Imports** — agregar `useRef`:
```tsx
import { useState, useEffect, useRef } from 'react'
```

**`ModalPanel`** — agregar ref y useEffect de focus:
```tsx
function ModalPanel({ fromDialect, sourceSql, onClose, onApply }: Omit<ConversionModalProps, 'open'>) {
  const { targets, toDialect, setToDialect } = useTargetDialect(fromDialect)
  const { convertedSQL, changes } = convertDialect(sourceSql, fromDialect, toDialect)
  const { copied, handleCopy } = useCopy(convertedSQL)
  const panelRef = useRef<HTMLDivElement>(null)

  useEffect(() => { panelRef.current?.focus() }, [])

  const fromLabel = DIALECTS.find(d => d.value === fromDialect)?.label ?? fromDialect
  const toLabel   = DIALECTS.find(d => d.value === toDialect)?.label   ?? toDialect

  return (
    <motion.div
      ref={panelRef}
      tabIndex={-1}
      initial={{ opacity: 0, scale: 0.95, y: -10 }}
      animate={{ opacity: 1, scale: 1, y: 0 }}
      exit={{ opacity: 0, scale: 0.95 }}
      transition={{ duration: 0.15 }}
      role="dialog" aria-modal="true" aria-label="Convert SQL dialect"
      style={{
        position: 'fixed', top: '50%', left: '50%', transform: 'translate(-50%, -50%)',
        zIndex: 999, background: 'var(--surface)', border: '1px solid var(--border-hi)',
        borderRadius: 12, boxShadow: '0 16px 48px rgba(0,0,0,0.6)',
        width: 560, maxHeight: '80vh', display: 'flex', flexDirection: 'column',
        outline: 'none',
      }}
    >
      <ModalHeader fromLabel={fromLabel} toDialect={toDialect} targets={targets} onToChange={setToDialect} onClose={onClose} />
      <ChangesSummary changes={changes} toLabel={toLabel} />
      <SqlPreview sql={convertedSQL} />
      <ModalFooter copied={copied} onCopy={handleCopy} onApply={() => { onApply(convertedSQL, toDialect); onClose() }} />
    </motion.div>
  )
}
```

**`ModalFooter`** — envolver texto del Copy en span aria-live:
```tsx
function ModalFooter({ copied, onCopy, onApply }: {
  copied:  boolean
  onCopy:  () => void
  onApply: () => void
}) {
  return (
    <div style={{ display: 'flex', gap: 8, justifyContent: 'flex-end', padding: '12px 16px', borderTop: '1px solid var(--border)' }}>
      <button
        onClick={onCopy}
        style={{ padding: '6px 12px', borderRadius: 6, fontSize: 11, background: 'var(--elevated)', border: '1px solid var(--border)', cursor: 'pointer', color: 'var(--text-2)', display: 'flex', alignItems: 'center', gap: 4 }}
      >
        {copied ? <Check size={12} /> : <Copy size={12} />}
        <span role="status" aria-live="polite" aria-atomic="true">
          {copied ? 'Copied!' : 'Copy'}
        </span>
      </button>
      <button
        onClick={onApply}
        style={{ padding: '6px 14px', borderRadius: 6, fontSize: 11, background: 'var(--a)', border: 'none', cursor: 'pointer', fontWeight: 700, color: '#000' }}
      >
        Use this query
      </button>
    </div>
  )
}
```

- [ ] **Step 1: Escribir test**

Crear `src/__tests__/editor/ConversionModal.a11y.test.tsx`:
```tsx
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'

vi.mock('framer-motion', () => ({
  motion: { div: ({ children, ...p }: React.ComponentProps<'div'>) => <div {...p}>{children}</div> },
  AnimatePresence: ({ children }: { children: React.ReactNode }) => <>{children}</>,
}))

vi.mock('@/components/editor/DialectSelector', () => ({
  DIALECTS: [
    { value: 'postgresql', label: 'PostgreSQL' },
    { value: 'mysql', label: 'MySQL' },
  ],
}))

vi.mock('@/lib/converter/dialect-converter', () => ({
  convertDialect: () => ({ convertedSQL: 'SELECT 1', changes: [] }),
}))

import { ConversionModal } from '@/components/editor/ConversionModal'

const defaultProps = {
  open: true,
  fromDialect: 'postgresql' as const,
  sourceSql: 'SELECT 1',
  onClose: vi.fn(),
  onApply: vi.fn(),
}

describe('ConversionModal a11y', () => {
  it('renders dialog with role=dialog', () => {
    render(<ConversionModal {...defaultProps} />)
    expect(screen.getByRole('dialog')).toBeTruthy()
  })

  it('Copy button has aria-live region', () => {
    render(<ConversionModal {...defaultProps} />)
    const liveRegion = document.querySelector('[aria-live="polite"]')
    expect(liveRegion).toBeTruthy()
    expect(liveRegion?.textContent).toBe('Copy')
  })

  it('dialog panel has tabIndex=-1 for focus', () => {
    render(<ConversionModal {...defaultProps} />)
    const dialog = screen.getByRole('dialog')
    expect(dialog.getAttribute('tabindex')).toBe('-1')
  })
})
```

- [ ] **Step 2: Verificar que falla**

```
npm run test -- src/__tests__/editor/ConversionModal.a11y.test.tsx
```

Expected: FAIL

- [ ] **Step 3: Aplicar cambios a ConversionModal.tsx**

1. Cambiar línea 1: `import { useState, useEffect, useRef } from 'react'`
2. En `ModalPanel` — agregar `const panelRef = useRef<HTMLDivElement>(null)` y `useEffect(() => { panelRef.current?.focus() }, [])`
3. En el `motion.div` de ModalPanel: agregar `ref={panelRef} tabIndex={-1}` y `outline: 'none'` al style
4. En `ModalFooter`: envolver texto Copy en `<span role="status" aria-live="polite" aria-atomic="true">`

- [ ] **Step 4: Correr tests**

```
npm run test -- src/__tests__/editor/ConversionModal.a11y.test.tsx
```

Expected: 3 PASS

- [ ] **Step 5: TypeScript**

```
npx tsc --noEmit
```

- [ ] **Step 6: Commit**

```
git add src/components/editor/ConversionModal.tsx src/__tests__/editor/ConversionModal.a11y.test.tsx
git commit -m "feat(a11y): auto-focus conversion modal on open + aria-live on Copy button"
```

---

## Task 4: Collections UX — Confirm Delete + Auto-select + Tags Placeholder

**Files:**
- Modify: `src/components/editor/CollectionPicker.tsx` — confirm antes de delete, hasCollections en empty state
- Modify: `src/components/editor/SaveQueryForm.tsx` — auto-select nueva colección, fix placeholder tags

### Cambios en `CollectionPicker.tsx`

**`CollectionHeader`** — agregar confirm antes de llamar onDelete:
```tsx
function CollectionHeader({ col, onDelete }: { col: SavedCollection; onDelete: (id: string) => void }) {
  const handleDelete = () => {
    if (window.confirm(`Delete collection "${col.name}" and all its queries?`)) {
      onDelete(col.id)
    }
  }
  return (
    <div style={{ padding: '6px 10px', fontSize: 10, fontWeight: 700, color: 'var(--text-3)', textTransform: 'uppercase', letterSpacing: '0.09em', display: 'flex', alignItems: 'center', justifyContent: 'space-between', borderBottom: '1px solid var(--border)', background: 'var(--elevated)' }}>
      <span>{col.name} ({col.queries.length})</span>
      {col.id !== 'col_recent_migrated' && (
        <button onClick={handleDelete} aria-label={`Delete collection ${col.name}`} style={{ background: 'none', border: 'none', cursor: 'pointer', color: '#E24B4A', padding: 2 }}>
          <Trash2 size={10} />
        </button>
      )}
    </div>
  )
}
```

**`DropdownList`** — agregar prop `hasCollections` para mensaje diferenciado:
```tsx
function DropdownList({ filtered, search, hasCollections, onSelect, onDelete }: {
  filtered: SavedCollection[]
  search: string
  hasCollections: boolean
  onSelect: (q: SavedQuery) => void
  onDelete: (id: string) => void
}) {
  const emptyMsg = search
    ? 'No queries match your search'
    : hasCollections
      ? 'No queries saved in any collection yet'
      : 'No saved queries yet — create a collection to get started'

  return (
    <div style={{ overflowY: 'auto', flex: 1 }}>
      {filtered.length === 0 && (
        <div style={{ padding: 16, textAlign: 'center', fontSize: 12, color: 'var(--text-3)' }}>
          {emptyMsg}
        </div>
      )}
      {filtered.map(col => (
        <CollectionGroup key={col.id} col={col} onSelect={onSelect} onDelete={onDelete} />
      ))}
    </div>
  )
}
```

**En `CollectionPicker`** — pasar `hasCollections` a `DropdownList`:

Cambiar la línea que renderiza `DropdownList`:
```tsx
: <><DropdownSearch search={search} onChange={setSearch} onShowSave={() => setShowSave(true)} /><DropdownList filtered={filtered} search={search} hasCollections={collections.length > 0} onSelect={handleSelect} onDelete={removeCollection} /></>
```

### Cambios en `SaveQueryForm.tsx`

**`handleCreateCollection`** en `useSaveQueryForm` — auto-seleccionar nueva colección:
```ts
const handleCreateCollection = () => {
  if (!newColName.trim()) return
  const trimmed = newColName.trim()
  addCollection(trimmed)
  setNewColName('')
  const newCol = useAppStore.getState().collections.find(c => c.name === trimmed)
  if (newCol) setColId(newCol.id)
}
```

**Tags placeholder** — cambiar de `"reporting, slow, draft"` a `"tag1, tag2, tag3"`:
```tsx
<FormField label="Tags (comma-separated)">
  <input style={inputStyle} value={tags} onChange={e => setTags(e.target.value)} placeholder="tag1, tag2, tag3" />
</FormField>
```

- [ ] **Step 1: Escribir tests**

Crear `src/__tests__/editor/CollectionPicker.ux.test.tsx`:
```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'

vi.mock('@/store/useAppStore', () => ({ useAppStore: vi.fn() }))
vi.mock('react-dom', async () => {
  const mod = await vi.importActual<typeof import('react-dom')>('react-dom')
  return { ...mod, createPortal: (node: React.ReactNode) => node }
})

import { useAppStore } from '@/store/useAppStore'
import { CollectionPicker } from '@/components/editor/CollectionPicker'

const mockRemove = vi.fn()
const COLLECTIONS = [
  { id: 'col1', name: 'Work', queries: [{ id: 'q1', name: 'Q1', sql: 'SELECT 1', dialect: 'postgresql', tags: [] }] },
]

function mountPicker() {
  vi.mocked(useAppStore).mockImplementation((sel: (s: unknown) => unknown) =>
    sel({
      collections: COLLECTIONS,
      setQuery: vi.fn(), setDialect: vi.fn(),
      removeCollection: mockRemove,
    })
  )
  return render(<CollectionPicker />)
}

describe('CollectionPicker delete confirmation', () => {
  beforeEach(() => { vi.clearAllMocks(); vi.spyOn(window, 'confirm').mockReturnValue(false) })

  it('asks for confirmation before deleting collection', () => {
    mountPicker()
    fireEvent.click(screen.getByRole('button', { name: /query collections/i }))
    const deleteBtn = screen.getByRole('button', { name: /delete collection Work/i })
    fireEvent.click(deleteBtn)
    expect(window.confirm).toHaveBeenCalledWith('Delete collection "Work" and all its queries?')
    expect(mockRemove).not.toHaveBeenCalled()
  })

  it('deletes collection when user confirms', () => {
    vi.spyOn(window, 'confirm').mockReturnValue(true)
    mountPicker()
    fireEvent.click(screen.getByRole('button', { name: /query collections/i }))
    const deleteBtn = screen.getByRole('button', { name: /delete collection Work/i })
    fireEvent.click(deleteBtn)
    expect(mockRemove).toHaveBeenCalledWith('col1')
  })
})
```

Crear `src/__tests__/editor/SaveQueryForm.ux.test.tsx`:
```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'

vi.mock('@/store/useAppStore', () => ({ useAppStore: vi.fn() }))
vi.mock('@/store/useAppStore', () => {
  const addCollection = vi.fn()
  const mockGetState = vi.fn()
  const useAppStore = vi.fn()
  useAppStore.getState = mockGetState
  return { useAppStore, addCollection }
})

import { useAppStore } from '@/store/useAppStore'
import { SaveQueryForm } from '@/components/editor/SaveQueryForm'

function mountForm() {
  const addCollection = vi.fn()
  const mockCollections = [{ id: 'col_default', name: 'Default', queries: [] }]
  vi.mocked(useAppStore).mockImplementation((sel: (s: unknown) => unknown) =>
    sel({
      collections: mockCollections,
      saveQueryToCollection: vi.fn(),
      addCollection,
    })
  )
  ;(useAppStore as { getState: () => unknown }).getState = () => ({
    collections: [{ id: 'col_new', name: 'New Collection', queries: [] }, ...mockCollections],
  })
  return { result: render(<SaveQueryForm onClose={vi.fn()} />), addCollection }
}

describe('SaveQueryForm', () => {
  beforeEach(() => vi.clearAllMocks())

  it('shows tag placeholder as "tag1, tag2, tag3"', () => {
    mountForm()
    const tagInput = screen.getByPlaceholderText('tag1, tag2, tag3')
    expect(tagInput).toBeTruthy()
  })
})
```

- [ ] **Step 2: Verificar que falla**

```
npm run test -- src/__tests__/editor/CollectionPicker.ux.test.tsx src/__tests__/editor/SaveQueryForm.ux.test.tsx
```

Expected: FAIL

- [ ] **Step 3: Aplicar cambios a CollectionPicker.tsx**

1. Cambiar `CollectionHeader` button onClick para incluir confirm
2. Agregar `hasCollections` prop a `DropdownList` con mensaje diferenciado
3. Pasar `hasCollections={collections.length > 0}` desde `CollectionPicker`

- [ ] **Step 4: Aplicar cambios a SaveQueryForm.tsx**

1. Cambiar `handleCreateCollection` para auto-seleccionar con `useAppStore.getState()`
2. Cambiar placeholder de tags de `"reporting, slow, draft"` a `"tag1, tag2, tag3"`

- [ ] **Step 5: Correr tests**

```
npm run test -- src/__tests__/editor/CollectionPicker.ux.test.tsx src/__tests__/editor/SaveQueryForm.ux.test.tsx
```

Expected: PASS

- [ ] **Step 6: TypeScript**

```
npx tsc --noEmit
```

- [ ] **Step 7: Commit**

```
git add src/components/editor/CollectionPicker.tsx src/components/editor/SaveQueryForm.tsx src/__tests__/editor/CollectionPicker.ux.test.tsx src/__tests__/editor/SaveQueryForm.ux.test.tsx
git commit -m "feat(ux): confirm collection delete + auto-select new collection + cleaner tag placeholder"
```

---

## Task 5: Suggestions + Schema Panel UX

**Files:**
- Modify: `src/components/panels/SuggestionsPanel.tsx` — Apply siempre visible (disabled cuando no rewritable)
- Modify: `src/components/panels/SchemaPanel.tsx` — confirm antes de Clear Schema

### Cambios en `SuggestionsPanel.tsx`

**`ApplyButton`** — aceptar prop `rewritable`, mostrar disabled cuando es false:
```tsx
function ApplyButton({ id, title, onApply, rewritable }: {
  id: string; title: string; onApply: (id: string) => void; rewritable: boolean
}) {
  return (
    <button
      onClick={() => rewritable && onApply(id)}
      disabled={!rewritable}
      aria-label={rewritable ? `Apply suggestion: ${title}` : `Auto-apply not available: ${title}`}
      title={rewritable ? undefined : 'Auto-apply not available for this suggestion'}
      style={{
        fontSize: 10, padding: '2px 8px', borderRadius: 4,
        background: rewritable ? 'var(--a)' : 'var(--bg-elevated)',
        color: rewritable ? '#000' : 'var(--text-3)',
        border: rewritable ? 'none' : '1px solid var(--border)',
        cursor: rewritable ? 'pointer' : 'not-allowed',
        fontWeight: 600, display: 'flex', alignItems: 'center', gap: 3,
        opacity: rewritable ? 1 : 0.6,
      }}
    >
      <Check size={10} /> Apply
    </button>
  )
}
```

**`CardHeader`** — siempre renderizar `<ApplyButton>`, pasar `rewritable`:
```tsx
function CardHeader({ suggestion, onApply }: { suggestion: Suggestion; onApply: (id: string) => void }) {
  const catIcon = CATEGORY_ICON[suggestion.category] ?? <span>•</span>
  const impactColor = IMPACT_COLOR[suggestion.impact]

  return (
    <div style={{ display: 'flex', alignItems: 'center', gap: 8, marginBottom: 8 }}>
      <span style={{ fontSize: 12 }}>{catIcon}</span>
      <span style={{ fontSize: 12, fontWeight: 600, color: 'var(--text-primary)', flex: 1 }}>
        {suggestion.title}
      </span>
      <span style={{ fontSize: 10, padding: '1px 6px', borderRadius: 10, background: impactColor, color: '#fff', fontWeight: 600 }}>
        {suggestion.impact}
      </span>
      <ApplyButton
        id={suggestion.id}
        title={suggestion.title}
        onApply={onApply}
        rewritable={canRewrite(suggestion.id)}
      />
    </div>
  )
}
```

### Cambios en `SchemaPanel.tsx`

**`SchemaLoaded`** — agregar confirm antes de clearSchema:
```tsx
function SchemaLoaded({ tables }: { tables: [string, SchemaTable][] }) {
  const clearSchema = useAppStore((s) => s.clearSchema)

  const handleClear = () => {
    if (window.confirm('Clear schema? This will disable column validation until you reload it.')) {
      clearSchema()
    }
  }

  return (
    <div style={{ padding: 12, overflowY: 'auto', height: '100%' }}>
      <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: 10 }}>
        <span style={{ fontSize: 11, color: 'var(--text-2)' }}>
          {tables.length} table{tables.length !== 1 ? 's' : ''} loaded
        </span>
        <button
          onClick={handleClear}
          aria-label="Clear schema"
          style={{ fontSize: 10, padding: '2px 8px', borderRadius: 4, background: 'none', border: '1px solid var(--border)', cursor: 'pointer', color: '#E24B4A', display: 'flex', alignItems: 'center', gap: 4 }}
        >
          <X size={10} /> Clear
        </button>
      </div>
      {tables.map(([name, table]) => <TableTree key={name} tableName={name} table={table} />)}
    </div>
  )
}
```

- [ ] **Step 1: Escribir tests**

Crear `src/__tests__/panels/SuggestionsPanel.ux.test.tsx`:
```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen } from '@testing-library/react'

vi.mock('@/store/useAppStore', () => ({ useAppStore: vi.fn() }))
vi.mock('@/lib/optimizer/rewriter', () => ({
  canRewrite: (id: string) => id === 'rewritable-id',
  applyRewrite: vi.fn(),
}))

import { useAppStore } from '@/store/useAppStore'
import { SuggestionsPanel } from '@/components/panels/SuggestionsPanel'

const SUGGESTIONS = [
  { id: 'rewritable-id', title: 'Use index', category: 'index', impact: 'high', reason: 'r', before: 'b', after: 'a' },
  { id: 'non-rewritable', title: 'Add cache', category: 'performance', impact: 'low', reason: 'r', before: 'b', after: 'a' },
]

function mountPanel() {
  vi.mocked(useAppStore).mockImplementation((sel: (s: unknown) => unknown) =>
    sel({ suggestions: SUGGESTIONS, query: 'SELECT 1', setQuery: vi.fn(), undoRewrite: vi.fn(), previousQuery: null })
  )
  return render(<SuggestionsPanel />)
}

describe('SuggestionsPanel Apply button', () => {
  beforeEach(() => vi.clearAllMocks())

  it('always renders Apply button for every suggestion', () => {
    mountPanel()
    const applyButtons = screen.getAllByRole('button', { name: /apply/i })
    expect(applyButtons).toHaveLength(2)
  })

  it('Apply button is disabled for non-rewritable suggestions', () => {
    mountPanel()
    const [rewritableBtn, nonRewritableBtn] = screen.getAllByRole('button', { name: /apply/i })
    expect(rewritableBtn).not.toBeDisabled()
    expect(nonRewritableBtn).toBeDisabled()
  })

  it('non-rewritable Apply button has helpful title', () => {
    mountPanel()
    const [, nonRewritableBtn] = screen.getAllByRole('button', { name: /apply/i })
    expect(nonRewritableBtn.title).toBe('Auto-apply not available for this suggestion')
  })
})
```

Crear `src/__tests__/panels/SchemaPanel.ux.test.tsx`:
```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'

vi.mock('@/store/useAppStore', () => ({ useAppStore: vi.fn() }))

import { useAppStore } from '@/store/useAppStore'
import { SchemaPanel } from '@/components/panels/SchemaPanel'

const MOCK_SCHEMA = {
  users: {
    columns: [{ name: 'id', type: 'INT', isPrimaryKey: true, isForeignKey: false, isIndexed: false, nullable: false }],
    indexes: [],
    primaryKeys: ['id'],
    foreignKeys: [],
  },
}

function mountLoaded() {
  const clearSchema = vi.fn()
  vi.mocked(useAppStore).mockImplementation((sel: (s: unknown) => unknown) =>
    sel({ schema: MOCK_SCHEMA, clearSchema, loadSchema: vi.fn() })
  )
  return { clearSchema, result: render(<SchemaPanel />) }
}

describe('SchemaPanel clear confirmation', () => {
  beforeEach(() => vi.clearAllMocks())

  it('asks for confirmation before clearing schema', () => {
    vi.spyOn(window, 'confirm').mockReturnValue(false)
    const { clearSchema } = mountLoaded()
    fireEvent.click(screen.getByRole('button', { name: /clear schema/i }))
    expect(window.confirm).toHaveBeenCalled()
    expect(clearSchema).not.toHaveBeenCalled()
  })

  it('clears schema when user confirms', () => {
    vi.spyOn(window, 'confirm').mockReturnValue(true)
    const { clearSchema } = mountLoaded()
    fireEvent.click(screen.getByRole('button', { name: /clear schema/i }))
    expect(clearSchema).toHaveBeenCalled()
  })
})
```

- [ ] **Step 2: Verificar que fallan**

```
npm run test -- src/__tests__/panels/SuggestionsPanel.ux.test.tsx src/__tests__/panels/SchemaPanel.ux.test.tsx
```

Expected: FAIL

- [ ] **Step 3: Aplicar cambios a SuggestionsPanel.tsx**

1. Agregar prop `rewritable` a `ApplyButton` con estilos disabled
2. En `CardHeader`: siempre renderizar `<ApplyButton>` con `rewritable={canRewrite(suggestion.id)}`

- [ ] **Step 4: Aplicar cambios a SchemaPanel.tsx**

1. En `SchemaLoaded`: crear `handleClear` con confirm, llamar `handleClear` en el botón onClick

- [ ] **Step 5: Correr tests**

```
npm run test -- src/__tests__/panels/SuggestionsPanel.ux.test.tsx src/__tests__/panels/SchemaPanel.ux.test.tsx
```

Expected: PASS

- [ ] **Step 6: TypeScript**

```
npx tsc --noEmit
```

- [ ] **Step 7: Commit**

```
git add src/components/panels/SuggestionsPanel.tsx src/components/panels/SchemaPanel.tsx src/__tests__/panels/SuggestionsPanel.ux.test.tsx src/__tests__/panels/SchemaPanel.ux.test.tsx
git commit -m "feat(ux): always show Apply button (disabled when not rewritable) + confirm clear schema"
```

---

## Task 6: Accessibility Quick Wins

**Files:**
- Modify: `src/components/layout/PanelRight.tsx` — aria-label en badges
- Modify: `src/components/diagram/ComplexityBadge.tsx` — Escape key cierra breakdown
- Modify: `src/components/ui/Toast.tsx` — role=status + aria-live
- Modify: `src/components/editor/DialectSelector.tsx` — disable ConvertButton sin query + DROPDOWN_H dinámico

### Cambios en `PanelRight.tsx`

Agregar `aria-label` a los badges de Issues y Suggestions:

Issues badge:
```tsx
<span
  aria-label={`${errorCount + warnCount} issue${errorCount + warnCount !== 1 ? 's' : ''}`}
  style={{
    marginLeft: 4, fontSize: 10, padding: '0 4px',
    background: errorCount > 0 ? '#E24B4A' : '#EF9F27',
    color: '#fff', borderRadius: 10, fontWeight: 600,
  }}
>
  {errorCount + warnCount}
</span>
```

Suggestions badge:
```tsx
<span
  aria-label={`${suggestions.length} suggestion${suggestions.length !== 1 ? 's' : ''}`}
  style={{
    marginLeft: 4, fontSize: 10, padding: '0 4px',
    background: '#1D9E75', color: '#fff', borderRadius: 10, fontWeight: 600,
  }}
>
  {suggestions.length}
</span>
```

### Cambios en `ComplexityBadge.tsx`

Agregar `useEffect` para cerrar con Escape. Cambiar imports:
```tsx
import { useState, useEffect } from 'react'
```

Agregar en `ComplexityBadge` antes del return:
```tsx
useEffect(() => {
  if (!open) return
  const handler = (e: KeyboardEvent) => { if (e.key === 'Escape') setOpen(false) }
  document.addEventListener('keydown', handler)
  return () => document.removeEventListener('keydown', handler)
}, [open])
```

### Cambios en `Toast.tsx`

Agregar `role="status"` y `aria-live="polite"` al `motion.div`:
```tsx
<motion.div
  role="status"
  aria-live="polite"
  aria-atomic="true"
  initial={{ opacity: 0, y: 8, scale: 0.95 }}
  animate={{ opacity: 1, y: 0, scale: 1 }}
  exit={{ opacity: 0, y: 8, scale: 0.95 }}
  transition={{ duration: 0.18 }}
  style={{ ... }}
>
  {message}
</motion.div>
```

### Cambios en `DialectSelector.tsx`

**`ConvertButton`** — deshabilitar cuando no hay query:
```tsx
function ConvertButton({ value, query, onQueryChange, onChange }: {
  value:          Dialect
  query:          string
  onQueryChange?: (q: string) => void
  onChange:       (d: Dialect) => void
}) {
  const [open, setOpen] = useState(false)
  const hasQuery = query.trim().length > 0
  return (
    <>
      <button
        onClick={() => hasQuery && setOpen(true)}
        disabled={!hasQuery}
        aria-label="Convert SQL to another dialect"
        title={hasQuery ? 'Convert to another dialect' : 'Type a SQL query first to convert'}
        style={{
          background: 'var(--elevated)', border: '1px solid var(--border)', borderRadius: 6,
          padding: '4px 7px', cursor: hasQuery ? 'pointer' : 'not-allowed',
          display: 'flex', alignItems: 'center', gap: 4, fontSize: 11,
          color: 'var(--text-2)', opacity: hasQuery ? 1 : 0.45,
        }}
      >
        <ArrowLeftRight size={12} />
      </button>
      <ConversionModal
        open={open}
        fromDialect={value}
        sourceSql={query}
        onClose={() => setOpen(false)}
        onApply={(sql, dialect) => { onQueryChange?.(sql); onChange(dialect) }}
      />
    </>
  )
}
```

**`DROPDOWN_H`** — calcular dinámicamente:
```tsx
const DROPDOWN_H = DIALECTS.length * 61 + 8
```

- [ ] **Step 1: Escribir tests**

Crear `src/__tests__/a11y/a11y-quick-wins.test.tsx`:
```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'

// Mock framer-motion
vi.mock('framer-motion', () => ({
  motion: {
    div: ({ children, ...p }: React.ComponentProps<'div'>) => <div {...p}>{children}</div>,
  },
  AnimatePresence: ({ children }: { children: React.ReactNode }) => <>{children}</>,
}))

vi.mock('@/store/useAppStore', () => ({ useAppStore: vi.fn() }))

import { useAppStore } from '@/store/useAppStore'

// ── Toast tests ──────────────────────────────────────────────────────────────
import { Toast } from '@/components/ui/Toast'

describe('Toast a11y', () => {
  it('has role=status and aria-live=polite', () => {
    render(<Toast message="Copied!" visible onHide={vi.fn()} />)
    const el = screen.getByRole('status')
    expect(el.getAttribute('aria-live')).toBe('polite')
    expect(el.textContent).toBe('Copied!')
  })
})

// ── ComplexityBadge Escape ────────────────────────────────────────────────────
import { ComplexityBadge } from '@/components/diagram/ComplexityBadge'

describe('ComplexityBadge Escape key', () => {
  beforeEach(() => vi.clearAllMocks())

  it('closes breakdown panel on Escape', () => {
    vi.mocked(useAppStore).mockImplementation((sel: (s: unknown) => unknown) =>
      sel({
        complexityResult: {
          level: 'Simple', score: 2,
          breakdown: { tableCount: 1, joinCount: 0, subqueryCount: 0, cteCount: 0, setOpCount: 0, procedureCount: 0, aggregateCount: 0, conditionCount: 0, loopCount: 0 },
        },
      })
    )
    render(<ComplexityBadge />)
    // Abrir el panel
    fireEvent.click(screen.getByRole('button', { name: /query complexity/i }))
    expect(screen.getByText('Complexity Breakdown')).toBeTruthy()
    // Cerrar con Escape
    fireEvent.keyDown(document, { key: 'Escape' })
    expect(screen.queryByText('Complexity Breakdown')).toBeNull()
  })
})

// ── PanelRight badge aria-labels ──────────────────────────────────────────────
import { PanelRight } from '@/components/layout/PanelRight'

// Mock all panels
vi.mock('@/components/panels/GlossaryPanel', () => ({ GlossaryPanel: () => <div>G</div> }))
vi.mock('@/components/panels/IssuesPanel',   () => ({ IssuesPanel: () => <div>I</div> }))
vi.mock('@/components/panels/SuggestionsPanel', () => ({ SuggestionsPanel: () => <div>S</div> }))
vi.mock('@/components/panels/LineagePanel',  () => ({ LineagePanel: () => <div>L</div> }))
vi.mock('@/components/panels/SchemaPanel',   () => ({ SchemaPanel: () => <div>SC</div> }))

describe('PanelRight badge aria-labels', () => {
  beforeEach(() => vi.clearAllMocks())

  it('issues badge has aria-label', () => {
    vi.mocked(useAppStore).mockImplementation((sel: (s: unknown) => unknown) =>
      sel({
        issues: [{ id: '1', severity: 'error', message: 'e' }, { id: '2', severity: 'warning', message: 'w' }],
        suggestions: [],
      })
    )
    render(<PanelRight />)
    const badge = document.querySelector('[aria-label*="issue"]')
    expect(badge?.getAttribute('aria-label')).toBe('2 issues')
  })

  it('suggestions badge has aria-label', () => {
    vi.mocked(useAppStore).mockImplementation((sel: (s: unknown) => unknown) =>
      sel({
        issues: [],
        suggestions: [{ id: 's1' }, { id: 's2' }, { id: 's3' }],
      })
    )
    render(<PanelRight />)
    const badge = document.querySelector('[aria-label*="suggestion"]')
    expect(badge?.getAttribute('aria-label')).toBe('3 suggestions')
  })
})
```

- [ ] **Step 2: Verificar que fallan**

```
npm run test -- src/__tests__/a11y/a11y-quick-wins.test.tsx
```

Expected: FAIL

- [ ] **Step 3: Aplicar cambio en Toast.tsx**

Agregar `role="status" aria-live="polite" aria-atomic="true"` al `motion.div`.

- [ ] **Step 4: Aplicar cambio en ComplexityBadge.tsx**

1. Agregar `useEffect` al import
2. Agregar handler Escape dentro de `ComplexityBadge` (antes del return)

- [ ] **Step 5: Aplicar cambios en PanelRight.tsx**

Agregar `aria-label` a ambos badges.

- [ ] **Step 6: Aplicar cambios en DialectSelector.tsx**

1. Cambiar `const DROPDOWN_H = 244` a `const DROPDOWN_H = DIALECTS.length * 61 + 8`
2. Actualizar `ConvertButton` con guard `hasQuery` y `disabled={!hasQuery}`

- [ ] **Step 7: Correr tests**

```
npm run test -- src/__tests__/a11y/a11y-quick-wins.test.tsx
```

Expected: PASS

- [ ] **Step 8: TypeScript**

```
npx tsc --noEmit
```

- [ ] **Step 9: Commit**

```
git add src/components/ui/Toast.tsx src/components/diagram/ComplexityBadge.tsx src/components/layout/PanelRight.tsx src/components/editor/DialectSelector.tsx src/__tests__/a11y/a11y-quick-wins.test.tsx
git commit -m "feat(a11y): toast aria-live, complexity Escape, badge aria-labels, convert guard"
```

---

## Task 7: Glossary Search

**Files:**
- Modify: `src/components/panels/GlossaryPanel.tsx` — agregar input de búsqueda y filtrado

### Cambio requerido

El archivo completo resultante:
```tsx
import { useState } from 'react'
import { Search } from 'lucide-react'
import { useAppStore } from '@/store/useAppStore'

export function GlossaryPanel() {
  const parseResult = useAppStore((s) => s.parseResult)
  const glossary = parseResult?.glossary ?? []
  const [search, setSearch] = useState('')

  if (glossary.length === 0) {
    return (
      <div style={{ padding: 16, color: 'var(--text-secondary)', fontSize: 13, textAlign: 'center' }}>
        Parse a SQL query to see the glossary.
      </div>
    )
  }

  const q = search.trim().toLowerCase()
  const filtered = q
    ? glossary.filter(e =>
        e.keyword.toLowerCase().includes(q) ||
        e.role.toLowerCase().includes(q) ||
        e.detail.toLowerCase().includes(q)
      )
    : glossary

  return (
    <div style={{ display: 'flex', flexDirection: 'column', height: '100%' }}>
      <div style={{ padding: '8px 12px', borderBottom: '1px solid var(--border)', display: 'flex', alignItems: 'center', gap: 6 }}>
        <Search size={12} style={{ color: 'var(--text-3)', flexShrink: 0 }} />
        <input
          value={search}
          onChange={e => setSearch(e.target.value)}
          placeholder="Filter keywords..."
          aria-label="Filter glossary"
          style={{ flex: 1, background: 'none', border: 'none', outline: 'none', fontSize: 11, color: 'var(--text-1)' }}
        />
      </div>
      <div style={{ flex: 1, padding: 12, overflowY: 'auto' }}>
        {filtered.length === 0 && (
          <div style={{ padding: 16, textAlign: 'center', fontSize: 12, color: 'var(--text-3)' }}>
            No keywords match &quot;{search}&quot;
          </div>
        )}
        {filtered.map((entry, i) => (
          <div
            key={i}
            style={{
              marginBottom: 12,
              padding: '10px 12px',
              background: 'var(--bg-elevated)',
              borderRadius: 6,
              borderLeft: '3px solid var(--accent)',
            }}
          >
            <div style={{ display: 'flex', alignItems: 'center', gap: 8, marginBottom: 4 }}>
              <code style={{
                fontSize: 12, fontWeight: 700,
                color: 'var(--accent)',
                background: 'var(--bg-surface)',
                padding: '1px 6px', borderRadius: 4,
              }}>
                {entry.keyword}
              </code>
              <span style={{ fontSize: 11, color: 'var(--text-secondary)' }}>{entry.role}</span>
              {entry.lineRef && (
                <span style={{ fontSize: 10, color: 'var(--text-secondary)', marginLeft: 'auto' }}>
                  line {entry.lineRef}
                </span>
              )}
            </div>
            <p style={{ margin: 0, fontSize: 12, color: 'var(--text-primary)', lineHeight: 1.5 }}>
              {entry.detail}
            </p>
          </div>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 1: Escribir tests**

Crear `src/__tests__/panels/GlossaryPanel.search.test.tsx`:
```tsx
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'

vi.mock('@/store/useAppStore', () => ({ useAppStore: vi.fn() }))

import { useAppStore } from '@/store/useAppStore'
import { GlossaryPanel } from '@/components/panels/GlossaryPanel'

const GLOSSARY = [
  { keyword: 'SELECT', role: 'Clause', detail: 'Retrieves columns from tables', lineRef: 1 },
  { keyword: 'FROM',   role: 'Clause', detail: 'Specifies the source table',    lineRef: 1 },
  { keyword: 'WHERE',  role: 'Filter', detail: 'Filters rows by condition',      lineRef: 2 },
]

function mountPanel() {
  vi.mocked(useAppStore).mockImplementation((sel: (s: unknown) => unknown) =>
    sel({ parseResult: { glossary: GLOSSARY } })
  )
  return render(<GlossaryPanel />)
}

describe('GlossaryPanel search', () => {
  beforeEach(() => vi.clearAllMocks())

  it('renders search input when glossary is non-empty', () => {
    mountPanel()
    expect(screen.getByRole('textbox', { name: /filter glossary/i })).toBeTruthy()
  })

  it('shows all entries when search is empty', () => {
    mountPanel()
    expect(screen.getByText('SELECT')).toBeTruthy()
    expect(screen.getByText('FROM')).toBeTruthy()
    expect(screen.getByText('WHERE')).toBeTruthy()
  })

  it('filters by keyword', () => {
    mountPanel()
    fireEvent.change(screen.getByRole('textbox', { name: /filter glossary/i }), { target: { value: 'sel' } })
    expect(screen.getByText('SELECT')).toBeTruthy()
    expect(screen.queryByText('FROM')).toBeNull()
    expect(screen.queryByText('WHERE')).toBeNull()
  })

  it('filters by role (case-insensitive)', () => {
    mountPanel()
    fireEvent.change(screen.getByRole('textbox', { name: /filter glossary/i }), { target: { value: 'filter' } })
    expect(screen.getByText('WHERE')).toBeTruthy()
    expect(screen.queryByText('SELECT')).toBeNull()
  })

  it('filters by detail text', () => {
    mountPanel()
    fireEvent.change(screen.getByRole('textbox', { name: /filter glossary/i }), { target: { value: 'source table' } })
    expect(screen.getByText('FROM')).toBeTruthy()
    expect(screen.queryByText('SELECT')).toBeNull()
  })

  it('shows empty state when no results', () => {
    mountPanel()
    fireEvent.change(screen.getByRole('textbox', { name: /filter glossary/i }), { target: { value: 'xyz-not-found' } })
    expect(screen.getByText(/no keywords match/i)).toBeTruthy()
  })

  it('shows parse prompt when glossary is empty', () => {
    vi.mocked(useAppStore).mockImplementation((sel: (s: unknown) => unknown) =>
      sel({ parseResult: null })
    )
    render(<GlossaryPanel />)
    expect(screen.getByText(/parse a sql query/i)).toBeTruthy()
    expect(screen.queryByRole('textbox')).toBeNull()
  })
})
```

- [ ] **Step 2: Verificar que fallan**

```
npm run test -- src/__tests__/panels/GlossaryPanel.search.test.tsx
```

Expected: FAIL (no existe search input)

- [ ] **Step 3: Reemplazar GlossaryPanel.tsx**

Reemplazar el contenido completo con el código del bloque "Cambio requerido" de arriba.

- [ ] **Step 4: Correr tests**

```
npm run test -- src/__tests__/panels/GlossaryPanel.search.test.tsx
```

Expected: 7 PASS

- [ ] **Step 5: TypeScript**

```
npx tsc --noEmit
```

- [ ] **Step 6: Suite completa**

```
npm run test
```

Expected: todos pasan

- [ ] **Step 7: Commit**

```
git add src/components/panels/GlossaryPanel.tsx src/__tests__/panels/GlossaryPanel.search.test.tsx
git commit -m "feat(ux): add search/filter input to GlossaryPanel"
```

---

## Verificación final

Después de los 7 tasks, correr:

```
npm run test
npx tsc --noEmit
```

Expected: todos los tests pasan, TypeScript clean.

Luego hacer commit de cierre si todo está OK:
```
git commit --allow-empty -m "chore: ux-polish complete — 7 tasks, all tests passing"
```
